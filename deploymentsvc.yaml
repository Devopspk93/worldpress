apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-app6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: php-app6
  template:
    metadata:
      labels:
        app: php-app6
    spec:
      containers:
      - name: php-app6
        image: devopspk93/php-project
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: php-app6
spec:
  selector:
    app: php-app6
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort
